import type { Config } from "tailwindcss";

// Tailwind CSS v4 auto-detects template content via its Vite plugin, so this
// file only exists to give the project an explicit, documented place to add
// theme customization later (per the Phase 1 plan's directory layout).
export default {} satisfies Config;
