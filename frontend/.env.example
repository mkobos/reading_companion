# Public, non-secret configuration only — see repo Secrets Handling rule.
# Base URL the API client prepends to requests. Defaults to "/api" so the
# Vite dev proxy (see vite.config.ts) and the production single-origin
# deployment both work without CORS configuration.
VITE_API_BASE_URL=/api
